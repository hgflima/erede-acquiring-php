box: wercker/php
build:
  steps:
    - script:
        name: install dependencies
        code: |-
            composer install
    - script:
        name: Unit Tests
        code: vendor/bin/phpunit
